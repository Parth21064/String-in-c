this file is for string practice

🔹 BASIC STRING QUESTIONS (must-know)

What is a string in C? How is it stored in memory?

Why is '\0' important in C strings?

What is the difference between char str[] and char *str?

What happens if a string does not have a null character?

Why is gets() unsafe? What should be used instead?

Difference between strlen() and sizeof() for strings.

Can we modify a string literal? Why?

What is the return type of strlen()?

🔹 INTERMEDIATE STRING QUESTIONS (very common in interviews)

Write a program to find the length of a string without using strlen().

Write a program to reverse a string.

Check whether a string is a palindrome.

Count vowels, consonants, digits, and spaces in a string.

Write a program to copy one string to another without strcpy().

Write a program to compare two strings without strcmp().

Find the frequency of each character in a string.

Remove spaces from a string.

Convert a string from lowercase to uppercase.

Find the number of words in a string.

Check whether two strings are anagrams.

🔹 ADVANCED STRING QUESTIONS (technical round favorites ⭐)

Reverse a string using pointers.

Find the first non-repeating character in a string.

Remove duplicate characters from a string.

Find the longest word in a sentence.

Implement strcat() without using library functions.

Check if a string is a rotation of another string.

Replace a substring with another string.

Find all substrings of a given string.

Count occurrences of a substring.

Print all permutations of a string.

Implement your own strtok().

🔹 TRICKY / CONCEPTUAL QUESTIONS (interview traps ⚠️)

What is the output?

char *s = "Hello";
s[0] = 'h';


Difference between:

char s1[] = "Hello";
char *s2 = "Hello";


What happens if you pass a string to a function?

How are strings stored internally in C?

What is string immutability in C?

Explain buffer overflow with an example.

Why does scanf("%s", str) stop at space?

What is the difference between strncpy() and strcpy()?

🔹 CODING CHALLENGE (technical round level 🔥)

Reverse words in a sentence without reversing characters.

Find the longest palindromic substring.

Compress a string
Example: aaabbc → a3b2c1

Remove all special characters from a string.

Find the most frequent character in a string.